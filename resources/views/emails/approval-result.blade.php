<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ $success ? 'Salary Sheet Approved' : 'Approval Status' }}</title>
    <style>
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            background: #f3f4f6;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1rem;
        }
        .card {
            background: #fff;
            border-radius: 1rem;
            box-shadow: 0 4px 24px rgba(0,0,0,0.10);
            max-width: 480px;
            width: 100%;
            padding: 3rem 2.5rem;
            text-align: center;
        }
        .icon {
            font-size: 4rem;
            margin-bottom: 1.25rem;
            line-height: 1;
        }
        .icon-success { color: #10b981; }
        .icon-info    { color: #f59e0b; }
        h1 {
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: 0.75rem;
            color: #111827;
        }
        .sub-message {
            color: #6b7280;
            font-size: 0.95rem;
            margin-bottom: 2rem;
            line-height: 1.6;
        }
        .sheet-badge {
            display: inline-block;
            background: #f0fdf4;
            color: #065f46;
            border: 1px solid #6ee7b7;
            border-radius: 0.5rem;
            padding: 0.5rem 1.25rem;
            font-weight: 600;
            font-size: 1rem;
            margin-bottom: 1.5rem;
        }
        .sheet-badge-warn {
            background: #fffbeb;
            color: #92400e;
            border-color: #fcd34d;
        }
        .detail-row {
            display: flex;
            justify-content: space-between;
            padding: 0.6rem 0;
            border-bottom: 1px solid #f3f4f6;
            font-size: 0.875rem;
            text-align: left;
        }
        .detail-row:last-child { border-bottom: none; }
        .detail-label { color: #6b7280; }
        .detail-value { color: #111827; font-weight: 500; }
        .details-box {
            background: #f9fafb;
            border-radius: 0.5rem;
            padding: 1rem 1.25rem;
            margin-bottom: 1.5rem;
        }
        .status-badge {
            display: inline-block;
            padding: 0.2rem 0.75rem;
            border-radius: 9999px;
            font-size: 0.8rem;
            font-weight: 600;
        }
        .status-approve { background: #d1fae5; color: #065f46; }
        .status-other   { background: #fef3c7; color: #92400e; }
        footer {
            margin-top: 2rem;
            color: #9ca3af;
            font-size: 0.8rem;
        }
    </style>
</head>
<body>
    <div class="card">
        @if($success)
            <div class="icon icon-success">&#10003;</div>
            <h1>Approved!</h1>
            <div class="sheet-badge">{{ $salarySheet->sheet_no }}</div>
        @else
            <div class="icon icon-info">&#8505;</div>
            <h1>Already Processed</h1>
            <div class="sheet-badge sheet-badge-warn">{{ $salarySheet->sheet_no }}</div>
        @endif

        <p class="sub-message">{{ $message }}<br><span style="font-size:0.88rem;">{{ $subMessage ?? '' }}</span></p>

        @if(isset($salarySheet))
        <div class="details-box">
            @if($salarySheet->job)
            <div class="detail-row">
                <span class="detail-label">Job Number</span>
                <span class="detail-value">{{ $salarySheet->job->job_number ?? '—' }}</span>
            </div>
            <div class="detail-row">
                <span class="detail-label">Job Name</span>
                <span class="detail-value">{{ $salarySheet->job->job_name ?? '—' }}</span>
            </div>
            @endif
            <div class="detail-row">
                <span class="detail-label">Location</span>
                <span class="detail-value">{{ $salarySheet->location ?? '—' }}</span>
            </div>
            <div class="detail-row">
                <span class="detail-label">Status</span>
                <span class="detail-value">
                    <span class="status-badge {{ $salarySheet->status === 'approve' ? 'status-approve' : 'status-other' }}">
                        {{ ucfirst($salarySheet->status) }}
                    </span>
                </span>
            </div>
        </div>
        @endif

        <footer>This page was generated by the Salary Management System.</footer>
    </div>
</body>
</html>
